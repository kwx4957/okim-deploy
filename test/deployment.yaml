apiVersion: apps/v1
kind: Deployment
metadata:
  name: ohkim
  namespace: ohkim
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ohkim-api
  template:
    metadata:
      labels:
        app: ohkim-api
    spec:
      containers:
      - image: kmss6905/ohkim-api:0.05
        name: ohkim-api
        ports:
        - containerPort: 8085
          protocol: TCP
      imagePullSecrets:
      - name: dockersecret
