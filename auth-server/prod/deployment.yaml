apiVersion: apps/v1
kind: Deployment
metadata:
  name: ohkim-auth
  namespace: ohkim
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ohkim-auth
      tier: auth
  template:
    metadata:
      labels:
        app: ohkim-auth
        tier: auth
    spec:
      containers:
      - image: "kmss6905/ohkim-auth:1.0"
        name: ohkim-auth
        ports:
        - containerPort: 8082
          protocol: TCP
      imagePullSecrets:
      - name: dockersecret
